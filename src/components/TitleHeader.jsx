import React from 'react'

const TitleHeader = ({title}) => {
    return (
        <h1 className="text-4xl font-semibold underline text-blue-500 mb-5 text-center">{title}</h1>
    )
}

export default TitleHeader